# 手机双端改版 · Design QA

final result: passed

本次目标是按用户确认的方案组合四张手机参考图，并加入第五张图的设置模块；不是逐像素复制。保留浅色、青绿主色、白色卡片与适老聊天，深色设置参考转换为同一浅色系统。

## 视觉来源

- P1 看板：`/Users/gods./Library/Application Support/LarkShell/sdk_storage/0bed5e75c17bb2e4fd5f2e2c3aafc4ff/resources/images/img_v3_0215f_edb18db0-4e43-437a-85f7-a1c1b0eb4c6g.jpg`
- P2 患者：同目录 `img_v3_0215f_d9682132-281b-41a8-a837-0b3cc8389fag.png`
- P3 用药：同目录 `img_v3_0215f_157b8eac-e729-4293-8d0a-7dd1a1e0f6dg.jpg`
- P4 守护：`/var/folders/3q/h5js_s153kl4vwc9mvw9wb4w0000gn/T/codex-clipboard-758e3c2c-29d6-44ce-afbb-43f845db378e.png`
- 设置：`/var/folders/3q/h5js_s153kl4vwc9mvw9wb4w0000gn/T/codex-clipboard-04d159b8-422b-42ab-95c8-276aede3cd65.png`

## 比较证据

本地实现：`http://127.0.0.1:4317/`。独立流程测试使用4318及单独的 `output/mobile-qa-state.json`，未用测试结果覆盖主入口的家庭配置。主入口在原日常记录为空时载入了现有虚构样例。

证据目录：`output/mobile-qa/`。

- 全景：`desktop-guard-fixed.png`（1280×720），修复后 document 尺寸为1280×720，无横向或纵向页面溢出。
- 手机：`mobile-guard.png`、`mobile-patient.png`、`final-board.png`、`final-settings.png`、`final-preferences.png`（均390×844）。内部内容滚动，导航、输入和演示条保持可达。
- 同一比较输入：`compare-board.png`、`compare-patient.png`、`compare-guard.png`、`compare-settings.png`。
- 用药重点区域：`medication-full.png`、`medication-rect.json`、`medication-detail.png`、`compare-medication-final.png`。先保存完整浏览器截图，再依据DOM矩形裁剪，避免截图工具clip输出的缩放偏差。
- 双端：`final-dual-fixed.png`；已确认双端可见，无横向溢出；收起后只剩1个手机窗口。

参考图保留完整内容，按宽390px等比归一比较；实际手机截图为390 CSS px宽、390px图片宽（有效密度1）。不同参考图省略的状态栏、外围舞台及底部演示控件不作为视觉差异。参考数字和对话均为静态样例，实现使用当前周期与状态，未硬编码14/13/1。设置图保留整体布局比对，页面中的7项偏好通过滚动和表单逐项可达。

## 修正历史

1. [P2 已修复] 720px高电脑屏幕中，手机最小高度导致外层页面溢出，点击场景按钮后手机顶部被滚出视口。调整桌面高度计算与短屏间距，手机内滚动；`desktop-fall.png`为修复前，`desktop-guard-fixed.png`为修复后。修复后实测scrollHeight=innerHeight=720。
2. [P2 已修复] 双端模式隐藏了原侧栏中的收起按钮。将“收起对照”移至手机上方固定的角色工具条；`final-dual-fixed.png`为后验截图。浏览器实测关闭按钮可见、关闭后手机数量为1。
3. [P2 已修复] 关注卡的无事件记录缺少直接可查依据。补充按recordIds展示日期、值、来源和家属补充，以及跟进说明表单；浏览器打开样例睡眠记录可见保存跟进按钮。
4. [P2 已修复] 家属“联系爸爸”与患者“联系家人”原先共享求助入口。分为家属模拟终端对讲和患者应用内求助；双端操作语义现已区分。

## 五项视觉检查

- 字体：中文系统字体与DM Sans数字搭配，患者正文18px，大按钮和语音主操作；看护页面建立标题、正文、来源说明三级层次。聊天长句自然换行。
- 间距：保留参考中的白色圆角卡片、青绿小结和三列用药统计；窗口外角色及演示控件独立。短屏与390px宽实测无横向溢出。
- 颜色：青绿用于可操作项和完成态，红色只用于危险或提醒，琥珀色用于待核实。设置模块有意采用浅色以统一全产品。
- 图片：客厅与透明人物使用生成位图，人物完整、透明边缘正常，动画用于场景演示。地图沿用项目既有的本地几何图。标准图标沿用已有Lucide体系；不以参考截图作为整个APP背景。
- 文案：患者收到提醒不等于确认服药；统计区分已记录服用、待核实、明确未服用。无位置更新时标注待更新，重置不标为安全。删除虚构的“Agent已学习128条”，知识库计数来自实际资料。

## 交互与检查

- 患者找眼镜对话、消息同步、播放入口、打字入口、语音开关。
- 用药：收到提醒→本人提交动作线索→看护端待核实→家属确认→统计更新，浏览器完整通过。
- 跌倒：行走/失衡/倒地→事件确认→暂停→患者求助→家人接手→到场→误报核实与医疗模拟任务处理→保存结案，浏览器完整通过。
- 位置：轨迹推进→位置异常→接手→找到老人→说明并结案，浏览器完整通过。最新路线样例先连续偏离，再越出围栏；服务端测试通过。
- 设备：浏览器添加电子血压计（离线）并保存成功。作息表单、知识库、7项设置入口可达。服务端测试确认无效配置回滚与有效配置持久化。
- 7/30天切换、原始记录与完整报告入口、可下载摘要、双端展开/收起。
- 浏览器控制台最后检查无error。当前测试浏览器的系统语音播放不可用时出现可读提示；未宣称验证真实语音识别或真实硬件。
- `npm run check`通过116项测试与生产构建。后续仅界面修正已重新构建，并在浏览器复查。

## 可接受差异与后续

P3：动画为照片位图的动作模拟，未实现关节级3D动作；与模拟标签一致。真实摄像头推理、GPS、电话/推送、方言音色仍属于原项目接入边界。完整病历导入与复杂计划助手保留在完整工作台，手机端提供健康档案与摘要查看。

无未解决的P0/P1/P2。

## 第二轮优化验收（2026-09-12）

final result: passed

目标依据：用户确认实施上一轮六项优化建议。基线为本项目 `output/optimization-review/02-patient.png`、`03-board.png`，与最初P1–P4设计语言保持一致。

本轮证据在 `output/optimization-v2/`：
- `compare-patient.png`、`compare-board.png`：同一输入中的前后对照，来源与实现均按390×844归一（密度1）。用药首屏、颜色分级、点按语音、固定联系入口为有意变更。
- `hydration-patient.png`：从设置保存并体验后，患者端实际收到使用新称呼的提醒；点击“我知道了”后该提醒消失。
- `fall-new-pose.png`：新透明侧卧姿态与客厅画面，身体完整、无透明网格；正常、失衡、侧卧由同一事件时钟驱动。
- `tour-paused-mobile.png`、`tour-current.png`、`tour-completed.png`：三分钟脚本在浏览器完整跑完，阶段、角色、页面与最终看板正确。暂停后可继续，服务重启后恢复。
- `patient-320.png`：320×740窄屏；实测页面scrollWidth=320、scrollHeight=740。
- `board-first.png`：390×844，用药卡DOM范围y=369.84至602.14，完整出现在首屏。

修正历史：
1. [P2 已修复] 顶部讲解条引起1280×720页面多出5px高度；调整讲解模式下手机高度，后验scrollHeight=720。
2. [P1 已修复] 用药阶段的detail字段残留，遮挡后续跌倒画面；每个脚本阶段显式清除旧详情。`tour-desktop.png`保留修复前证据，`tour-current.png`与浏览器dialogs=[]为修复后证据，增加测试检查最终detail=null。
3. [P2 已修复] 初次聊天自动滚到底部，把普通问候挤出可视区；仅新聊天滚到底，新提醒/场景/告警滚到顶部。`patient-final.png`为后验。
4. [功能修复] 原提醒生成器固定写“周伯”，忽略饮水计划消息；限定家庭饮水管理计划采用已保存的称呼和预览消息，测试与浏览器均验证“周爷爷”生效。

五项检查：
- 字体：患者主操作17–18px，消息18px，快捷回复15px；新增固定联系入口16px。320px宽无横向溢出。
- 间距：用药卡提前，聊天区仍可独立滚动，输入和联系家人固定。手机、演示控制与讲解条在720px高桌面同屏可达。
- 色彩：日常青绿、待办琥珀、紧急红；普通问候已不再使用红色。
- 图片：使用新增真实透明位图 `elder-stumble.png`、`elder-on-floor.png`；alpha边界与地面位置经过视觉检查。生成的不同姿态存在轻微面部差异，属于虚构人物情境素材。
- 文案：摘要直接取实际记录中的夜晚/天数；来源计数与缺失日分开。自动核实记录带脚本模拟来源，不写入日常记录，自动事件结案使用demo_completed。

验证：`npm run check`通过；最终 `npm test` 122/122。新增完整演示、暂停恢复、配置中途关闭、较大围栏、饮水去重/撤回、称呼与方言原子保存测试。浏览器验证自动演示完整结束、换场无残留弹层、饮水设置→患者收到→确认、名称立即生效、首屏用药、侧卧姿态。最后检查控制台无error。

限制：真实ASR/音色取决于浏览器，本次未接受麦克风权限或实测真实老人发音。未宣称真实摄像头、GPS或外部电话/推送已接入。无未解决的P0/P1/P2。

## 找路演示与应答语音验收

final result: passed

参考：用户上传的 `/Users/gods./Library/Application Support/LarkShell/sdk_storage/0bed5e75c17bb2e4fd5f2e2c3aafc4ff/resources/images/img_v3_0215g_e52f2de8-0070-4da7-b2ee-ab37e9ad097g.jpg`，采用其中患者聊天、目的地确认、三步路线与约5分钟陪同卡的结构；保留当前手机容器、联系家人入口及真实来源提示。

本轮证据在 `output/wayfinding-qa/`：`final-route.png`、`final-route-controls.png`、`final-mobile.png`、`directions-verified.wav`、`check-final.log`。

- 路线按“确认目的地→三步指导→本人确认到达”执行，确认前不生成指路卡，可更换目的地；未知地点先提供选择。
- 已修复长路线卡自动滚到底部的问题，改为新路线展示时对齐消息顶部。第一步实测位于390px手机的y=297.93至345.93范围，保持可见。
- 固定操作区提供当前步骤按钮。第一步按钮行实测y=576.7至612.7，老人无需翻找历史消息。
- [已修复] 新增语音模块的原始fetch绑定在浏览器中报Illegal invocation；默认调用已改为函数包装，并重新构建、刷新验证。旧错误截图保留在过程文件，最终验证无该提示。
- 浏览器真实音频验证：目的地确认音频3.05秒，currentTime推进至末尾；整段路线音频曾记录18.86秒、currentTime=10.27且paused=false、readyState=4；到达回应7.21秒完整播放。后续共享播放器按句分段并预取，最新一段16.99秒、currentTime=9.49且正在播放。
- WAV验证：API返回200、audio/wav、本机provider；22050Hz单声道，验证语句4.79秒，RMS=4091.4，含真实非零音频采样。
- 关闭语音后页面中所有audio均paused=true。自动播放受限时保留待播音频并显示用户开启按钮；队列、缓存、迟到响应和百度返回类型均有测试。
- 最终生产构建与全套测试通过；最后检查为148项。测试期间保留项目同步更新的品牌和语音模块，验证覆盖最终整合版本。
- 390×844和320×740手机布局检查通过。当前本机合成声可用，未配置百度凭证；不将本机音频描述为已验证的百度云音色。路线为示例，不读取真实GPS。

五项视觉检查：中文字体与原手机界面一致；编号步骤与卡片间距清晰；青绿强调当前步骤；继续操作固定可达且原聊天按钮仍同高对齐；文案保留目的地确认、语音来源、路线模拟和本人到达确认的区别。
